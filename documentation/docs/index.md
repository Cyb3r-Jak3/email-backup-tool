# Email Backup Tool Documentation

Email Backup Tool is a command-line utility written in Go that allows users to back up their email accounts. It currently only supports IMAP. The tool can encrypt backups using age or OpenPGP and store them in S3-compatible storage.
